## {{ params.ecosystem_name }} {.unnumbered .parameter}

```{python}
#| tags: [parameters]
#| echo: false
# Parameters used in Python code blocks.
country_name = '{{ params.country_name }}'
ecosystem_names = {{ params.ecosystem_names }}
```

[Authors:]{.parameter} {{ ", ".join(params.authors) }}

```{python}
#| echo: false
from IPython.display import Markdown, display

if ecosystem_names:
    name_str = ", ".join([f"{ecosystem_name['name']} ({ecosystem_name['reference']})"
                for ecosystem_name in ecosystem_names])
    # display(Markdown(f'**{country_name} Ecosystem Names:** {name_str}'))
else:
    name_str = 'No ecosystem names available.'
```

[`{python} country_name` ecosystem names:]{.parameter} `{python} name_str`

[Global Classification:]{.parameter} {{ params.global_classification }}

[Functional Group:]{.parameter} {{ params.functional_group }}

[Global classification:]{.parameter} {{ params.global_classification }}

[IUCN Status:]{.parameter} {{ params.iucn_status }}

[Description:]{.parameter} {{ params.description }}

[Distribution:]{.parameter} {{ params.distribution }}

[Characteristic Native Biota:]{.parameter} {{ params.characteristic_native_biota }}

## Ecosystem Overview

This is an assessment report for {{ params.ecosystem_name }}.


## Assessment Details

*This section would contain detailed ecosystem assessment information including:*

- Distribution and extent
- Key ecological processes
- Characteristic species
- Threats and pressures
- Conservation status
- Management recommendations
